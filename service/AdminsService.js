'use strict';


/**
 * adds a joke item
 * Adds a joke to the system
 *
 * item Joke Joke object to add
 * no response value expected for this operation
 **/
exports.addJoke = function(item) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

